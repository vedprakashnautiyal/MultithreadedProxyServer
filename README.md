# MutlithreadedProxyServer
## Some Limitations
- Only suport HTTP version 1.0
- Is only implemented to handle GET request as it has easier implementation, however it can be extended to include POST request too.